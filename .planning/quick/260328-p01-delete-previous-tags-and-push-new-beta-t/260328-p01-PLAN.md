---
quick_id: "260328-p01"
mode: quick
date: 2026-03-28
---

# Quick Task 260328-p01: Delete previous tags and push new beta tag

## Task 1: Delete old tags (local + remote)

**Files:** None (git operations only)
**Action:**
1. Delete local tags: `v1.0` and `v1.1.0-beta.1`
2. Delete remote tags: `v1.0` and `v1.1.0-beta.1` from origin

**Verify:** `git tag -l` returns empty, `git ls-remote --tags origin` returns empty
**Done:** All old tags removed from local and remote

## Task 2: Create and push new v1.1.0-beta.1 tag

**Files:** None (git operations only)
**Action:**
1. Create annotated git tag `v1.1.0-beta.1` with message "Beta release v1.1.0-beta.1"
2. Push the tag to origin with `git push origin v1.1.0-beta.1`

**Verify:** `git tag -l "v1.1.0-beta.1"` returns the tag, and `git ls-remote --tags origin` shows it on remote
**Done:** Tag `v1.1.0-beta.1` exists locally and is pushed to origin
