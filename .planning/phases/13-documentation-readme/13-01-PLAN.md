---
phase: 13-documentation-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "A developer seeing the README understands what OpenClaw Desktop is and why it exists"
    - "A developer can set up the project locally using only the README instructions"
    - "A developer can build production binaries using only the README instructions"
    - "The README documents the full tech stack with specific versions"
    - "The README shows the project architecture (frontend/backend split, pages, commands)"
    - "The README lists all Tauri commands and frontend pages"
    - "The README includes prerequisites (Docker, Node.js, Rust toolchain)"
    - "The README has proper badges, structure, and formatting for an OSS project"
  artifacts:
    - path: "README.md"
      provides: "Complete project documentation"
      min_lines: 200
  key_links:
    - from: "README.md"
      to: "src/"
      via: "Project structure section listing all directories"
    - from: "README.md"
      to: "src-tauri/src/commands/"
      via: "Backend architecture section listing all 17 command modules"
    - from: "README.md"
      to: "package.json"
      via: "Prerequisites and setup instructions"
    - from: "README.md"
      to: "src-tauri/Cargo.toml"
      via: "Tech stack table with exact crate versions"
---

<objective>
Create a production-grade README.md for OpenClaw Desktop

**Purpose:** Replace the default Tauri template README with comprehensive documentation that enables developers to understand, set up, build, and contribute to the project without external references.

**Output:** Complete README.md with project overview, features, architecture, tech stack, prerequisites, setup instructions, build instructions, project structure, and configuration details.
</objective>

<execution_context>
@D:/projects/rust/openclaw-ins/.opencode/get-shit-done/workflows/execute-plan.md
@D:/projects/rust/openclaw-ins/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source files for understanding the project
@src-tauri/src/lib.rs
@src-tauri/Cargo.toml
@src-tauri/tauri.conf.json
@package.json
@src/router.tsx
@src/pages/
@src/hooks/
@src-tauri/src/commands/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deep codebase analysis — catalog all features, architecture, and tech stack</name>
  <files>README.md</files>
  <read_first>
    src-tauri/src/lib.rs (all 50+ Tauri command registrations)
    src-tauri/Cargo.toml (all Rust dependencies with versions)
    package.json (all frontend dependencies with versions)
    src/router.tsx (all 9 routes/pages)
    src/pages/ (all page files)
    src/hooks/ (all 13 custom hooks)
    src-tauri/src/commands/ (all 17 command modules)
    src-tauri/tauri.conf.json (app config, bundle settings)
    src/components/ (UI component categories)
  </read_first>
  <action>
    Analyze the entire codebase to produce a comprehensive inventory. Read every file listed above and catalog:

    1. **Pages (9 routes):** Dashboard (/), Setup Wizard (/setup), Docker (/docker), Install (/install), Configure (/configure), Monitor (/monitor), Channels (/channels), Settings (/settings), OpenClaw Webapp (/webapp)
    
    2. **Tauri commands (50+):** Group by domain:
       - Platform: get_platform_info
       - Docker: check_docker_health, get_docker_info, detect_docker
       - System: run_system_check
       - Install: install_openclaw, clean_install_dir, cancel_install
       - Verify: verify_installation
       - Config: read_config, write_config, validate_config
       - Monitoring: get_openclaw_status, get_agent_sessions, get_sandbox_containers, get_container_logs
       - Lifecycle: uninstall_openclaw, check_openclaw_update, update_openclaw
       - Channels: get_channels, disconnect_channel, connect_channel, get_whatsapp_qr, validate_telegram_token, validate_discord_token, get_contacts, update_contact_status, get_activity
       - Gateway: start_gateway, stop_gateway, restart_gateway, get_gateway_status, kill_gateway_on_port, gateway_ws_connect, gateway_ws_call, gateway_ws_disconnect
       - Node.js: check_nodejs, check_openclaw, check_prerequisites, install_openclaw_script, reinstall_openclaw
       - Models: fetch_provider_models
       - Control UI: open_control_ui, close_control_ui
       - Desktop Config: read_desktop_config, write_desktop_config, write_auth_profile, write_env_key
    
    3. **Rust backend modules:** commands/ (17 files), docker/ (check.rs, mod.rs), install/, error.rs, state.rs
    
    4. **Frontend hooks (13):** use-app-update, use-channels, use-config, use-docker-layer-progress, use-docker-logs, use-docker, use-gateway, use-install, use-models, use-monitoring, use-platform, use-uninstall, use-update
    
    5. **Frontend components:** channels/, config/, install/, layout/, status/, ui/, wizard/, system-check.tsx
    
    6. **Tech stack with exact versions from package.json and Cargo.toml**
    
    7. **Key architectural decisions from STATE.md:** Tauri v2, Docker-first sandboxing, bollard for Docker, Zustand + TanStack Query, shadcn/ui, motion for animations, HashRouter, etc.
    
    8. **Milestones completed:** v1.0 MVP (phases 1-6), v1.1 UX Polish & Channels (phases 7-10), v2.0 Gateway Integration (phase 12)
  </action>
  <verify>
    <automated>test -f README.md && echo "README exists" || echo "README missing"</automated>
  </verify>
  <done>Complete codebase inventory documented with all pages, commands, hooks, components, dependencies, and architectural decisions cataloged</done>
</task>

<task type="auto">
  <name>Task 2: Write production-grade README.md</name>
  <files>README.md</files>
  <read_first>
    src-tauri/src/lib.rs
    src-tauri/Cargo.toml
    package.json
    src/router.tsx
    src-tauri/tauri.conf.json
    src/pages/
    src/hooks/
    src-tauri/src/commands/
  </read_first>
  <action>
    Write a comprehensive, production-grade README.md. Structure:

    1. **Header:** Project name "OpenClaw Desktop", tagline about making OpenClaw accessible to non-technical users, badges (Tauri v2, React 19, TypeScript, License)

    2. **About:** What OpenClaw is (AI agent platform), what OpenClaw Desktop does (visual interface for installing, configuring, sandboxing, monitoring, updating). One paragraph value prop.

    3. **Features:** Bullet list covering:
       - One-click installation (Docker sandboxed or native)
       - Real-time monitoring (sessions, containers, logs)
       - Configuration management (YAML config, validation, hot-reload via Gateway)
       - Channel management (WhatsApp QR, Telegram/Discord bot tokens, contacts, activity)
       - Gateway integration (WebSocket JSON-RPC, live status, process management)
       - Setup wizard (config-first multi-step: model → API keys → sandbox → channels)
       - Auto-updates (self-updating via Tauri updater plugin)

    4. **Screenshots/GIFs:** Placeholder section (to be filled with actual screenshots)

    5. **Tech Stack:** Detailed table with exact versions:
       | Layer | Technology | Version | Purpose |
       |-------|-----------|---------|---------|
       | Runtime | Tauri v2 | 2.x | Desktop app framework |
       | Frontend | React | 19.x | UI framework |
       | Language | TypeScript | 5.8.x | Type safety |
       | Styling | Tailwind CSS | 4.2.x | Utility-first CSS |
       | Components | shadcn/ui + Radix | latest | Accessible UI primitives |
       | State | Zustand | 5.x | Client state management |
       | Server State | TanStack Query | 5.x | Async/cached state |
       | Animations | motion (Framer Motion) | 12.38.x | Page/micro animations |
       | Build | Vite + Rolldown | 8.x | Fast bundler |
       | Backend | Rust | stable | Tauri backend |
       | Docker API | bollard | 0.20.x | Docker management |
       | WebSocket | tokio-tungstenite | 0.26.x | Gateway connection |
       | HTTP | reqwest | 0.12.x | API client |

    6. **Prerequisites:** 
       - Node.js 22.14+ (recommended 24+)
       - pnpm (package manager)
       - Rust toolchain (rustup)
       - Docker Desktop (for sandboxed installation mode)
       - Platform-specific: Windows 10+ or Linux (macOS secondary)

    7. **Getting Started:** Step-by-step:
       - Clone repo
       - `pnpm install` (frontend deps)
       - `pnpm tauri dev` (run in dev mode)

    8. **Building:** Production build instructions:
       - `pnpm tauri build` (produces platform-specific binaries)
       - Output locations per platform

    9. **Project Structure:** Complete directory tree:
       ```
       openclaw-desktop/
       ├── src/                          # React frontend
       │   ├── pages/                    # 9 route pages
       │   ├── components/               # UI components (channels/, config/, install/, layout/, status/, ui/, wizard/)
       │   ├── hooks/                    # 13 custom React hooks
       │   ├── stores/                   # Zustand state stores
       │   ├── lib/                      # Utilities (animation presets, helpers)
       │   ├── config/                   # App configuration
       │   └── assets/                   # Static assets
       ├── src-tauri/                    # Rust backend
       │   ├── src/
       │   │   ├── commands/             # 17 Tauri command modules
       │   │   ├── docker/               # Docker health checks
       │   │   ├── install/              # Installation engine
       │   │   ├── lib.rs                # App entry, plugin registration
       │   │   ├── state.rs              # Shared app state
       │   │   └── error.rs              # Error types
       │   ├── capabilities/             # Tauri security capabilities
       │   └── tauri.conf.json           # Tauri configuration
       ├── package.json
       ├── Cargo.toml
       └── vite.config.ts
       ```

    10. **Tauri Commands Reference:** Table of all commands grouped by domain (Platform, Docker, System, Install, Verify, Config, Monitoring, Lifecycle, Channels, Gateway, Node.js, Models, Control UI, Desktop Config)

    11. **Configuration:** How config works — YAML config files, desktop_config commands, Gateway hot-reload

    12. **Development:** Dev server (`pnpm tauri dev`), frontend-only (`pnpm dev`), Rust rebuild triggers

    13. **Roadmap:** Links to milestones (v1.0 ✅, v1.1 ✅, v2.0 in progress)

    14. **Contributing:** Basic contribution guidelines (fork, branch, PR)

    15. **License:** Placeholder (MIT or project choice)

    Use the versions from package.json and Cargo.toml for the tech stack table. Use the actual command names from lib.rs. Use the actual file structure from the codebase. Every section must reference real project specifics — NO generic boilerplate.
  </action>
  <verify>
    <automated>rg -c "Tauri|React|TypeScript|Tailwind|bollard|Zustand|TanStack" README.md | head -1</automated>
  </verify>
  <done>README.md is 200+ lines with project-specific content covering features, tech stack, architecture, setup, build, and project structure — no default template text remains</done>
</task>

</tasks>

<verification>
Overall checks:
- [ ] README.md replaces the default template
- [ ] All 9 pages documented
- [ ] All 50+ Tauri commands listed
- [ ] All 13 frontend hooks referenced
- [ ] Tech stack matches package.json and Cargo.toml versions
- [ ] Project structure matches actual directory layout
- [ ] Prerequisites match actual dependencies
- [ ] Build instructions reference correct package manager (pnpm)
- [ ] No placeholder/template text remains ("This template should help...")
</verification>

<success_criteria>
- [ ] README.md is 200+ lines of project-specific content
- [ ] Every tech stack version matches package.json or Cargo.toml
- [ ] All 17 command modules referenced by name
- [ ] All 9 routes listed with descriptions
- [ ] Setup instructions use pnpm (project's package manager)
- [ ] Build instructions reference `pnpm tauri build`
- [ ] Project structure tree matches actual directory layout
- [ ] Features section covers all major capabilities (Docker, Gateway, Channels, Monitoring)
- [ ] No generic Tauri template boilerplate remains
</success_criteria>

<output>
After completion, create `.planning/phases/13-documentation-readme/13-01-SUMMARY.md`
</output>
