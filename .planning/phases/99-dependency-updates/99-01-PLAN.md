---
phase: 99-dependency-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src-tauri/Cargo.toml
  - src-tauri/Cargo.lock
autonomous: true
requirements: []

must_haves:
  truths:
    - "vite is updated to 8.0.3 in pnpm-lock.yaml"
    - "vitest is updated to 4.1.2 in pnpm-lock.yaml"
    - "uuid minor update applied in Cargo.lock"
    - "Frontend builds successfully after npm updates"
    - "Rust compiles successfully after uuid update"
  artifacts:
    - path: "package.json"
      provides: "npm dependency declarations"
    - path: "pnpm-lock.yaml"
      provides: "locked npm dependency versions"
    - path: "src-tauri/Cargo.toml"
      provides: "Rust dependency declarations"
    - path: "src-tauri/Cargo.lock"
      provides: "locked Rust dependency versions"
  key_links:
    - from: "package.json"
      to: "pnpm-lock.yaml"
      via: "pnpm install"
      pattern: "pnpm install"
---

## Objective

Apply safe dependency patches (within semver range) from Dependabot PRs #6 and Cargo minor group.

**Purpose:** Low-risk updates that don't require code changes. Dependabot PR #6 bumps vite 8.0.2→8.0.3 and vitest 4.1.1→4.1.2 (both within `^8.0.2` and `^4.1.1` ranges). The Cargo rust-minor group bumps uuid.

**Output:** Updated lock files reflecting latest patch/minor versions.

## Context

@package.json
@src-tauri/Cargo.toml

Dependabot PR #6: `deps(node): bump the minor group with 2 updates`
- vite: 8.0.2 → 8.0.3 (bug fixes, rolldown rc.12 update)
- vitest: 4.1.1 → 4.1.2 (flatted CVE fix, mock resolution fix)

Cargo rust-minor group: uuid minor version bump.

These are already within semver ranges specified in package.json/Cargo.toml, so no version specifiers need changing — only lock file updates.

## Tasks

<task type="auto">
  <name>Task 1: Update npm lock file for vite and vitest patches</name>
  <files>pnpm-lock.yaml</files>
  <action>Run `pnpm install` to update pnpm-lock.yaml with vite 8.0.3 and vitest 4.1.2. The version ranges in package.json (`^8.0.2` and `^4.1.1`) already permit these versions — only the lock file needs updating.</action>
  <verify>pnpm install exits 0. Check: grep for vite@8.0.3 and vitest@4.1.2 in pnpm-lock.yaml</verify>
  <done>pnpm-lock.yaml updated with vite 8.0.3 and vitest 4.1.2</done>
</task>

<task type="auto">
  <name>Task 2: Update Cargo.lock for uuid minor bump</name>
  <files>src-tauri/Cargo.lock</files>
  <action>Run `cargo update -p uuid` in src-tauri to update the uuid crate to latest compatible version. The Cargo.toml specifies `uuid = { version = "1", features = ["v4"] }` so any 1.x update is compatible.</action>
  <verify>cargo check exits 0 in src-tauri directory</verify>
  <done>Cargo.lock updated with latest uuid 1.x</done>
</task>

<task type="auto" type="checkpoint:human-verify">
  <name>Task 3: Verify build passes after safe updates</name>
  <files>pnpm-lock.yaml, src-tauri/Cargo.lock</files>
  <action>Run `pnpm build` (which runs `tsc && vite build`) to verify the frontend compiles. Then run `cargo check` in src-tauri to verify Rust compilation.</action>
  <verify>
    <automated>cd src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>Both frontend and Rust compilation succeed with no new errors</done>
</task>

## Verification

1. `pnpm install` completes without errors
2. `pnpm build` (tsc + vite build) succeeds
3. `cargo check` in src-tauri succeeds
4. No new warnings introduced

## Success Criteria

- vite 8.0.3 and vitest 4.1.2 in pnpm-lock.yaml
- uuid updated in Cargo.lock
- Full build passes (frontend + Rust)

## Output

After completion, commit: `deps: apply safe dependency patches (vite 8.0.3, vitest 4.1.2, uuid minor)`
